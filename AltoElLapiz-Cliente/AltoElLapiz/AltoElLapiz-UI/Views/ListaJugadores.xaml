<Page
    x:Class="AltoElLapizUI.ListaJugadores"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AltoElLapizUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#2B9CF5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock x:Uid="TextBlockJugadores" Grid.Row="0" TextAlignment="Center" Foreground="White" FontSize="70" Margin="0 10 0 0"></TextBlock>
        
        <!--Lista de jugadores-->
        <Rectangle Grid.Row="1" RadiusX="20" RadiusY="20" Width="500" Height="450" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 25 0 0" Fill="AntiqueWhite"></Rectangle>

        <ListView x:Name="listViewPartidas" ItemsSource="{Binding listadoUsuariosPartida}" Grid.Row="1" Width="500" Height="450">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel>
                        <TextBlock x:Name="jugador" Text="{Binding nick}" Foreground="Black"></TextBlock>
                      
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Uid="ListaJugadoresTextoEspera" Opacity="{Binding opacidadTextoEspera}" HorizontalAlignment="Center" Grid.Row="2" FontSize="15" Margin="0 0 0 20" Height="90" Width="350" Foreground="White" FontWeight="ExtraBold"></TextBlock>
        <Button x:Uid="ButtonJugar" Opacity="{Binding opacidadBtnJugar}" Background="White" HorizontalAlignment="Center" Grid.Row="2" FontSize="50" Margin="0 0 0 20" Height="90" Width="350" Command="{Binding jugar}"></Button>

       
        <!--Contenedor Chat-->
        <Frame x:Name="contenedorChat" Grid.Column="1" Grid.RowSpan="3" MaxWidth="400">

        </Frame>

    </Grid>
</Page>
